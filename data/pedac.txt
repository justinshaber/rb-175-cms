1. Signed out user sees "sign in" link on home page.
  - sign in link routes to "/users/signin"


2. "/users/signin"
  - Username label and input
  - password label and input
  - sign in button
3. Submitting sign in
  - Username: admin, Password: secret
  - if successful
    - redirected to home
    - session[:success] = "Welcome!"
    - "Signed in as admin" with 'Sign Out' button at bottom
  - if unsuccessful
    - redisplay login page
    - invalid username should appear in username input
    - session[:error] = "Invalid Credentials"
4. 'Sign Out' button
  - signs user out
  - shows sign out home page
  - session[:success] = "You have been signed out"